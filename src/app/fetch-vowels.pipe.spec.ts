import { FetchVowelsPipe } from './fetch-vowels.pipe';

describe('FetchVowelsPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchVowelsPipe();
    expect(pipe).toBeTruthy();
  });
});
